version: 1.0.{build}

image: Visual Studio 2019

platform:
  - x64

configuration: Release

build_script:
  - cmd: >
      pip install numpy pygame pillow utils pyinstaller
      pyinstaller main.spec

artifacts:
  - path: dist\game.exe
    name: game-exe
